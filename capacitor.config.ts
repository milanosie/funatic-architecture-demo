import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'funatic.mobile',
  appName: 'mobile',
  webDir: 'dist/mobile',
  bundledWebRuntime: false
};

export default config;
